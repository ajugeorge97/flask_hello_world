apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mypvc
spec:
  resources:
    requests:
      storage: {{ .Values.pvc.storage }}
  accessModes:
    - ReadWriteOnce
  # storageClassName: ""
